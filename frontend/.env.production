# Production environment variables
# These will be embedded in the build at compile time

# API URL - served via Cloud HTTPS Load Balancer → Cloud Run (northamerica-northeast1)
VITE_API_BASE_URL=https://api.taverns.red/api
